# Auto-provisions the Prometheus datasource so the bundled dashboard has
# something to query the moment Grafana starts.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
