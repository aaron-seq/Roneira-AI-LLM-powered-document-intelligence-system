# Core FastAPI and web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.8.2
pydantic-settings==2.3.0

# Authentication and Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# Database
sqlalchemy==2.0.23
aiosqlite==0.19.0
# Async PostgreSQL driver. Required whenever DATABASE_URL uses
# postgresql+asyncpg:// — which docker-compose does by default. It was
# commented out while compose still pointed at PostgreSQL, so the container
# failed at startup with "the asyncio extension requires an async driver".
asyncpg==0.29.0
alembic==1.13.1

# Redis/Caching (unified Redis client with async support)
redis[hiredis]==5.0.1

# Local LLM Integration (Ollama)
# No additional dependencies needed - Ollama runs as separate service

# OpenAI SDK (for Ollama compatibility and future extensibility)
openai==1.30.1

# LangChain for RAG and document processing
langchain==0.2.14
langchain-openai==0.1.8
langchain-community==0.2.12
langchain-core==0.2.33

# RAG Pipeline (Vector Search & Embeddings)
sentence-transformers>=2.2.0
chromadb>=0.4.0
numpy>=1.24.0
tiktoken>=0.5.0

# HTTP and API clients
httpx==0.25.2

# Rate limiting
slowapi>=0.1.9

# File processing
Pillow>=10.1.0
pdfplumber==0.10.3
python-docx==1.1.0
python-magic==0.4.27
# OCR for scanned pages and image uploads. This is only the Python wrapper —
# the actual work is done by the tesseract binary, which is a system package
# (see the OCR section of the README). Without it the service still runs and
# reports OCR as unavailable rather than failing. Page rasterisation uses
# pypdfium2, which already ships as a pdfplumber dependency.
pytesseract>=0.3.10

# Utilities
python-dotenv==1.0.0
click==8.1.7
typer>=0.12.3

# Monitoring and logging
structlog==23.2.0
prometheus-client==0.19.0

# Testing (dev dependencies)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
Faker>=20.0.0
hypothesis>=6.90.0
mutmut>=2.4.4
scikit-learn>=1.3.2
selenium>=4.15.0
debugpy>=1.8.0
structlog>=23.2.0

# Code quality
# ruff is pinned because pyproject.toml selects rules (e.g. ASYNC240) that do
# not exist in older releases: an unpinned `pip install ruff` made `ruff check`
# fail to parse the config locally while passing in CI.
ruff==0.16.0
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1

# Production server
gunicorn==21.2.0

# WebSocket support
websockets==12.0

# JSON handling
orjson>=3.9.14
aiofiles
